# 2026-06-27 Worklog

## Requested Change

Patch this fork so the main binary updates from the fork, game resources update from original Maa resources, update UI is quiet, and a GitHub Actions workflow keeps the fork synced and released.

## Changes Made

- Added fork release URLs and original MaaResource version URL in `MaaUrls`.
- Added GitHub resource version checking so the overseas source can auto-download resource updates.
- Rewired resource auto-update so `Github` updates from original MaaResource and `MirrorChyan` stays on MirrorChyan.
- Centralized toast suppression in `ToastNotification.Show()` instead of replacing individual updater toast call sites.
- Reduced fork-specific `Instances.TaskQueueViewModel.AddLog` replacements in updater files back to upstream-style toast calls.
- Suppressed the first-boot-after-update changelog window.
- Centralized restart/update prompt suppression in `VersionUpdateDialogViewModel.AskToRestartCore()`.
- Changed app binary update checks to read fork GitHub releases instead of the upstream Maa API/MirrorChyan path.
- Required an exact OTA asset for automatic binary updates and stopped automatic full-package fallback.
- Disabled the external updater progress window while preserving a blocking failure message.
- Added `.github/workflows/fork-sync-build-release.yml` for upstream sync, Windows x64 build, fork release publishing, and OTA generation.
- Updated the workflow to generate seed OTA packages from recent upstream releases so the first fully automatic run can publish usable OTA assets even when no previous fork release exists.
- Normalized OTA source versions from downloaded package filenames and added `MaaRelease` recent releases as seed OTA sources.
- Changed the fork sync workflow schedule from weekly to daily at 02:30 Asia/Singapore.
- Added a scheduled-run skip gate so no-change daily runs stop after upstream comparison instead of building and publishing duplicate releases.
- Added a fork guard to upstream `Release Pipeline` (`.github/workflows/ci.yml`) so fork pushes do not run the upstream multi-platform release build.
- Cancelled unintended `Release Pipeline` run `28279342040` after pushing the quiet-update refactor.
- Manually dispatched and then cancelled workflow run `28270084608` because it used the earlier workflow revision before the seed-source normalization.
- Manually dispatched corrected workflow run `28270903362` on `dev-v2`.
- Confirmed workflow run `28270903362` completed successfully:
  - `Sync Upstream and Build Windows x64` succeeded.
  - `Generate OTA and Publish Release` succeeded.
- Confirmed release `v2026.6.26002` was published at `https://github.com/Mion354D/MaaAssistantArknights/releases/tag/v2026.6.26002`.
- Confirmed release `v2026.6.26002` contains one full Windows x64 package and 14 OTA packages seeded from recent upstream/fork package history.
- Added `doc/2026-06-27-fork-update-design.md`.

## Validation

- `git diff --check` passed.
- Fetched `MaaResource/main/resource/version.json` and confirmed it contains `last_updated` in the expected `yyyy-MM-dd HH:mm:ss.fff` format.
- `dotnet build src\MaaWpfGui\MaaWpfGui.csproj -c Release -p:Platform=x64 --no-restore` passed with 0 warnings and 0 errors after the initial restore.
- Local CMake validation could not be run because `cmake` is not installed in this workspace shell.
- Local GitHub CLI workflow validation could not be run because `gh` is not installed in this workspace shell.
- Committed and pushed the patch to `origin/dev-v2`.
- Verified in Chrome that GitHub registered `Fork Sync Build Release` at `/actions/workflows/fork-sync-build-release.yml` with a `workflow_dispatch` Run workflow control.
- Verified through the GitHub Actions API that run `28270903362` completed with `conclusion: success`.
- Verified through the GitHub Releases API that release `v2026.6.26002` is published and includes `MAA-v2026.6.26002-win-x64.zip` plus OTA assets such as `MAAComponent-OTA-v6.12.3-alpha.1.d035.g8276849f1a_v2026.6.26002-win-x64.zip` and `MAAComponent-OTA-v6.13.0-beta.2.d028.g0239409ef4_v2026.6.26002-win-x64.zip`.
